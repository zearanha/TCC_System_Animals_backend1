services:
  api:
    build: .
    container_name: municipal_animais_api
    restart: unless-stopped
    environment:
      PORT: 3000
      DATABASE_URL: mongodb+srv://aranhafalcao10_db_user:ndophumRS1oTK4q2@systemanimals.3dczshl.mongodb.net/SystemAnimals?retryWrites=true&w=majority&appName=SystemAnimals
      MONGODB_URI: mongodb+srv://aranhafalcao10_db_user:ndophumRS1oTK4q2@systemanimals.3dczshl.mongodb.net/SystemAnimals?retryWrites=true&w=majority&appName=SystemAnimals
      CORS_ORIGINS: http://localhost:3000,http://localhost:3001
      AUTH_SESSION_TTL_HOURS: 24
      DEFAULT_ADMIN_EMAIL: admin@municipio.local
      DEFAULT_ADMIN_PASSWORD: admin123456
      DEFAULT_ADMIN_NAME: Administrador do Sistema
      NOTIFICATION_SIMULATION: "${NOTIFICATION_SIMULATION:-false}"
      NOTIFICATION_EMAIL_ENABLED: "${NOTIFICATION_EMAIL_ENABLED:-true}"
      NOTIFICATION_WHATSAPP_ENABLED: "${NOTIFICATION_WHATSAPP_ENABLED:-true}"
      SMTP_HOST: "${SMTP_HOST:-smtp.gmail.com}"
      SMTP_PORT: "${SMTP_PORT:-587}"
      SMTP_SECURE: "${SMTP_SECURE:-false}"
      SMTP_USER: "${SMTP_USER:-}"
      SMTP_PASS: "${SMTP_PASS:-}"
      SMTP_FROM: "${SMTP_FROM:-Sistema Municipal <nao-responder@municipio.local>}"
      WHATSAPP_WEBHOOK_URL: "${WHATSAPP_WEBHOOK_URL:-}"
      WHATSAPP_WEBHOOK_TOKEN: "${WHATSAPP_WEBHOOK_TOKEN:-}"
    ports:
      - "3002:3000"
    volumes:
      - uploads_data:/app/uploads

volumes:
  uploads_data:
